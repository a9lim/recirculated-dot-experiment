# scripts/lab.sh queue scripts/example-queue.txt
# TAG <train args> [| <score args>]   — one run per line, in order.
# parity length 4 under D15 (cosine across the whole run, train-k {4..32}
# P~k, lam 0.125), both arms, snapshots every 500, then scored with a
# length-transfer readout.
parity4d15-wire --condition dots+wire --knobs length=4 --steps 8000 --cosine 8000 --snapshot-every 500 --eval-n 512 | --transfer length=5,6,8
parity4d15-dots --condition dots      --knobs length=4 --steps 8000 --cosine 8000 --snapshot-every 500 --eval-n 512 | --transfer length=5,6,8
